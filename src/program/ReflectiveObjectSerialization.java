package program;

public class ReflectiveObjectSerialization {
    private static Sender sender;

    public static void main(String[] args) {
        //Create server which the sender will use

        //create the receiver/client on A SEPARATE THREAD so we can have it all together

        //make sure to denote the visualizer output as different so ta can tell

        //sender = new Sender(true);
        //Client.connectAndSend("Hello, World!");
        //Server.receiveAndPrint();
    }
}
